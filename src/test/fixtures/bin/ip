#!/usr/bin/env bash

if [[ $2 == 'link' ]]; then
    cat "$(dirname $0)/ip-link-show-full.out"
else
    if [[ -z "$3" ]]; then
        cat "$(dirname $0)/ip-addr-show-full.out"
    else
        cat "$(dirname $0)/ip-addr-show.out"
    fi
fi